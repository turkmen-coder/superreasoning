/** Uygulama versiyonu — package.json ile senkronize tutulmalı. */
export const APP_VERSION = '3.2.0';
export const APP_DISPLAY_VERSION = `V${APP_VERSION.split('.').slice(0, 2).join('.')}`;
export const APP_NAME = 'Super Reasoning AI';
